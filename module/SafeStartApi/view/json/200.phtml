<?php

echo json_encode(
    $response = array(
        'meta' => array(
            'status' => 200,
            'requestId' => isset($this->requestId) ? $this->requestId : '',
        ),
        'data' => isset($this->answer) ? $this->answer : array('done' => false, 'error' => 'no answer')
    )
);
